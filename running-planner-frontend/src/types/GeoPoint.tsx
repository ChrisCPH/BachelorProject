export interface GeoPoint {
    latitude: number; // Latitude in decimal degrees
    longitude: number; // Longitude in decimal degrees
    maxDistanceMeters: number; // Maximum distance in meters from the point
}